import { createFileRoute } from "@tanstack/react-router";
import { useMemo,useState } from "react";
import { Search } from "lucide-react";
import { faculty } from "@/lib/content";
import { PageIntro } from "@/components/shared";
import { Input } from "@/components/ui/input";
export const Route=createFileRoute("/faculty")({head:()=>({meta:[{title:"Faculty — Northstar Institute"},{name:"description",content:"Meet Northstar's faculty across business, technology, design, and executive education."},{property:"og:title",content:"Faculty — Northstar Institute"},{property:"og:description",content:"Scholars, practitioners, and dedicated mentors."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:FacultyPage});
function FacultyPage(){const[q,setQ]=useState("");const[school,setSchool]=useState("All");const shown=useMemo(()=>faculty.filter(f=>(school==="All"||f.school===school)&&`${f.name} ${f.role} ${f.expertise.join(" ")}`.toLowerCase().includes(q.toLowerCase())),[q,school]);return <><PageIntro eyebrow="Faculty" title="Expertise with an open door." description="Learn from active researchers and practitioners who bring hard-won insight into every seminar, studio, and conversation."/><section className="py-16"><div className="site-container"><div className="grid gap-4 border-b border-border pb-8 md:grid-cols-[1fr_auto]"><div className="relative"><Search className="absolute left-4 top-1/2 size-5 -translate-y-1/2 text-muted-foreground"/><Input aria-label="Search faculty" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search by name or expertise" className="h-11 pl-12"/></div><select aria-label="Filter faculty by school" value={school} onChange={e=>setSchool(e.target.value)} className="h-11 border border-input bg-background px-4 text-sm">{["All","Business","Technology","Design","Executive Education"].map(x=><option key={x}>{x}</option>)}</select></div>{shown.length?<div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{shown.map((f,i)=><article key={f.name} className="border border-border bg-card p-7"><div className={`grid size-24 place-items-center rounded-full ${i%2?"bg-secondary":"bg-primary"} font-display text-2xl font-bold ${i%2?"text-secondary-foreground":"text-primary-foreground"}`}>{f.initials}</div><p className="eyebrow mt-8 text-primary">{f.school}</p><h2 className="mt-3 font-display text-2xl font-semibold">{f.name}</h2><p className="mt-2 text-sm text-muted-foreground">{f.role}</p><div className="mt-6 flex flex-wrap gap-2">{f.expertise.map(x=><span key={x} className="bg-secondary px-3 py-1 text-xs font-semibold">{x}</span>)}</div></article>)}</div>:<div className="py-20 text-center"><h2 className="font-display text-3xl font-semibold">No faculty found</h2><p className="mt-2 text-muted-foreground">Try another name or school.</p></div>}</div></section></>}